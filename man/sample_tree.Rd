% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_tree.R
\name{sample_tree}
\alias{sample_tree}
\title{Sample Nodes at Each Level of a Tree}
\usage{
sample_tree(
  tree,
  samples_per_level,
  method = c("per-parent", "level-wise"),
  seed = NULL
)
}
\arguments{
\item{tree}{A \code{data.tree} Node object returned by \code{\link{build_tree}}.}

\item{samples_per_level}{An integer vector specifying how many nodes to sample
at each tree level. Length should equal the number of nominal variables.
Use \code{NA} or value >= total available to keep all nodes at that level.}

\item{method}{Sampling method: \code{"per-parent"} (default) or \code{"level-wise"}.}

\item{seed}{An optional integer seed for reproducible sampling.}
}
\value{
A pruned \code{data.tree} Node object containing only the selected
nodes and their descendants.
}
\description{
Performs stratified random sampling on a hierarchical tree. At each level,
a specified number of child nodes are randomly selected from the available
nodes at that level. Two sampling strategies are supported:
}
\details{
\itemize{
\item \code{"level-wise"}: Sample N nodes uniformly from all nodes at each level,
regardless of parent. Simpler control but may produce imbalanced trees.
\item \code{"per-parent"} (default): For each selected parent node at level L,
sample N child nodes at level L+1. Preserves tree balance and ensures
coverage across all selected branches.
}
}
\examples{
\dontrun{
data(iris)
tree <- build_tree(iris, c("Species", "Sepal.Width"))
sampled <- sample_tree(tree, samples_per_level = c(2, 3))
print(sampled)
}

}
